function AdsensePlaceholder({ type = "banner" }) {
  return (
    <div className={`ad-placeholder ad-${type}`}>
      {/* Replace this with real AdSense code after approval */}
      <span>Ad space – place Google AdSense code here</span>
    </div>
  );
}

export default AdsensePlaceholder;
